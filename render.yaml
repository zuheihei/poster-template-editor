services:
  - type: web
    name: poster-template-editor
    runtime: node
    plan: free
    buildCommand: echo "No build required"
    startCommand: node server/static.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
